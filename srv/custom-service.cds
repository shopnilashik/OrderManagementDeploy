service CustomService {
    function importBaseData() returns {
        msg: String
    };

    function importBaseDataLocally() returns {
        msg: String
    };

    function importOrderType() returns {
        msg: String
    }
}
